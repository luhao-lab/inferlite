# M6 — API + SSE 服务化

> **状态**：✅ 完成（2026-08-23）
> **依赖**：M3 (Continuous Batching) · M4 (PagedAttention) · M5 (Prefix Cache)
> **主层**：L4 Server
> **对齐**：vLLM V1 AsyncLLM 架构（单进程简化版）

---

## 0. TL;DR

M6 将 inferlite 的推理引擎包装成 OpenAI-compatible HTTP 服务：

- `inferlite serve --model-dir /path/to/qwen3-0.6b` 启动服务
- `POST /v1/chat/completions` 支持流式/非流式两种模式
- SSE 流式输出逐 token 推送，格式对齐 OpenAI ChatCompletionStream
- 支持 temperature / top-k / top-p / repetition_penalty 采样参数
- Qwen3 thinking 模式的 `<think>...</think>` 内容映射到 `reasoning_content` 字段

### 核心机制

1. **AsyncEngine**：后台线程运行常驻 engine loop，动态接收请求、逐 token 推送结果
2. **FastAPI + SSE**：HTTP 端点接收请求，SSE 流式推送 chunk（`data: {json}\n\n`）
3. **SamplingProcessor**：替代 GreedySampler，支持可配置的采样策略
4. **Pydantic schemas**：请求/响应格式对齐 OpenAI spec，保证类型安全

### 架构对齐

```
vLLM V1（多进程）：
  API Server Process → AsyncLLM (asyncio) → EngineCore (独立进程) → GPU Workers

inferlite M6（单进程简化版）：
  FastAPI App → AsyncEngine (后台线程) → batch_generate_loop (复用 M3/M4/M5)
```

---

## 1. 背景：M5 的问题

M5 完成了 prefix cache + continuous batching + paged attention，但只能通过 Python API 调用：

```python
# M5：必须写 Python 代码
from inferlite.engine import batch_generate_paged

results = batch_generate_paged(
    model, sampler, prompts,
    max_new_tokens=100,
    num_blocks=128, block_size=16,
    config=config,
)
```

M6 目标：通过 HTTP API 调用，支持 curl / OpenAI SDK / ChatBox 等客户端：

```bash
# M6：curl 直接调用
curl http://localhost:8000/v1/chat/completions \
  -d '{"model":"qwen3","messages":[{"role":"user","content":"Hi"}],"stream":true}'

# OpenAI Python SDK
from openai import OpenAI
client = OpenAI(base_url="http://localhost:8000/v1", api_key="none")
response = client.chat.completions.create(
    model="qwen3",
    messages=[{"role": "user", "content": "Hi"}],
    stream=True,
)
for chunk in response:
    print(chunk.choices[0].delta.content, end="")
```

---

## 2. 目标与非目标

### 2.1 目标

| 能力 | M6 是否做 | vLLM V1 对应 |
|------|-----------|--------------|
| OpenAI-compatible API | ✅ | `vllm.entrypoints.openai.api_server` |
| 流式 SSE 输出 | ✅ | `StreamingResponse` + SSE chunks |
| 非流式完整响应 | ✅ | `ChatCompletionResponse` |
| temperature / top-k / top-p | ✅ | `SamplingParams` |
| repetition penalty | ✅ | `LogitsProcessor` |
| reasoning_content 映射 | ✅ | Qwen3 特有 |
| 常驻 engine loop | ✅ | `EngineCore` 常驻进程 |
| 动态请求接收 | ✅ | `AsyncLLM.add_request()` |

### 2.2 非目标

| 不做 | 原因 | 后续 |
|------|------|------|
| 多进程架构 | 单进程足够，简化实现 | M12+ 可扩展 |
| Token-budget scheduling | M3 已实现 FCFS | M10 chunked prefill |
| Speculative decoding | 不在 M6 范围 | M8 |
| Triton kernel | M9 范围 | M9 |
| 多 GPU TP/PP | 单 GPU 足够 | M12+ |
| 量化 | 不在 M6 范围 | M12+ |
| Web UI | CLI 足够，可选 Gradio/Streamlit | 可选 T6 |

---

## 3. 核心设计

### 3.1 架构总览

```
┌─ Main Thread (FastAPI + uvicorn) ─────────────────┐
│  POST /v1/chat/completions                        │
│    → parse request                                │
│    → engine.submit(prompt_ids, sampling_params)   │
│    → return (request_id, output_queue)            │
│                                                   │
│  if stream=true:                                  │
│    → StreamingResponse(_stream_response)          │
│    → async for token in output_queue:             │
│        yield f"data: {chunk_json}\n\n"            │
│    → yield "data: [DONE]\n\n"                     │
│                                                   │
│  if stream=false:                                 │
│    → await _complete_response                     │
│    → collect all tokens from output_queue         │
│    → return ChatCompletionResponse                │
└───────────────────────────────────────────────────┘
              ↕ queue.Queue (thread-safe)
┌─ Background Thread (Engine Loop) ─────────────────┐
│  while running:                                   │
│    # 接收新请求                                    │
│    while pending_queue.not_empty:                 │
│      req = pending_queue.get()                    │
│      scheduler.submit(req)                        │
│                                                   │
│    # admit + prefill + decode（复用 M3/M4/M5）     │
│    admitted = scheduler.admit()                   │
│    for req in admitted:                           │
│      prefill(req)                                 │
│      output_queue.put(first_token)                │
│                                                   │
│    running = scheduler.running                    │
│    for req in running:                            │
│      decode_step(req)                             │
│      output_queue.put(token)                      │
│      if finished:                                 │
│        output_queue.put(_Sentinel)                │
│        scheduler.mark_finished(req)               │
│                                                   │
│    sleep(0.001)  # 让出 CPU                        │
└───────────────────────────────────────────────────┘
```

### 3.2 线程模型

- **Main Thread**：FastAPI + uvicorn，处理 HTTP 请求
- **Background Thread**：engine loop，运行 admit → prefill → decode
- **通信**：`queue.Queue`（thread-safe），per-request output queue

**为什么用线程而不是 asyncio**：
- model forward 是 CPU/GPU bound，不适合 asyncio
- FastAPI 的 async handler 可以通过 `await asyncio.sleep()` 轮询 queue，不阻塞 event loop
- 线程间通过 `queue.Queue` 通信，避免 GIL 竞争（model forward 释放 GIL）

### 3.3 SamplingProcessor

替代 `GreedySampler`，支持可配置的采样策略：

```python
class SamplingParams:
    temperature: float = 0.0      # 0 = greedy
    top_k: int = -1               # -1 = 不过滤
    top_p: float = 1.0            # 1.0 = 不过滤
    repetition_penalty: float = 1.0  # 1.0 = 不惩罚
    seed: int | None = None       # 可复现采样

class SamplingProcessor:
    def __call__(self, logits: [B, V]) -> [B, 1]:
        # 1. repetition penalty（对已生成 token 降权）
        # 2. temperature scaling
        # 3. top-k filtering
        # 4. top-p (nucleus) filtering
        # 5. softmax → multinomial sample
```

**接口兼容**：`__call__(logits) -> [B, 1]`，可直接替换 `GreedySampler`。

### 3.4 OpenAI Schemas

Pydantic 模型，对齐 OpenAI spec：

```python
class ChatCompletionRequest(BaseModel):
    model: str = "qwen3"
    messages: list[ChatMessage]
    stream: bool = False
    temperature: float = 0.0
    top_p: float = 1.0
    top_k: int = -1
    max_tokens: int = 256
    repetition_penalty: float = 1.0
    seed: int | None = None

class ChatCompletionResponse(BaseModel):
    id: str
    object: str = "chat.completion"
    created: int
    model: str
    choices: list[ResponseChoice]
    usage: UsageInfo

class ChatCompletionStreamChunk(BaseModel):
    id: str
    object: str = "chat.completion.chunk"
    created: int
    model: str
    choices: list[StreamChoice]
```

### 3.5 思维链映射

Qwen3 thinking 模式输出：

```
<think>Let me think step by step...</think>
The answer is 42.
```

M6 提取 `<think>...</think>` 内容，映射到 `reasoning_content` 字段：

```python
def _split_thinking(text: str) -> tuple[str, str | None]:
    """提取 <think>...</think> 内容。"""
    if "<think>" not in text:
        return text, None

    # 找到最后一个完整的 <think>...</think>
    pattern = re.compile(r"<think>(.*?)</think>", re.DOTALL)
    matches = list(pattern.finditer(text))

    if not matches:
        # 未闭合的 </think>
        if "<think>" in text:
            start = text.index("<think>") + len("<think>")
            return "", text[start:]
        return text, None

    # 提取最后一个 think 块
    last = matches[-1]
    reasoning = last.group(1).strip()
    content = text[:last.start()] + text[last.end():]
    return content.strip(), reasoning
```

非流式响应：

```json
{
  "choices": [{
    "message": {
      "role": "assistant",
      "content": "The answer is 42.",
      "reasoning_content": "Let me think step by step..."
    }
  }]
}
```

流式响应暂不推送 `reasoning_content`（简化实现），全部作为 `content` 输出。

---

## 4. 实施任务卡

### T1: SamplingParams + SamplingProcessor

**前置**：M3/M4/M5 完成
**产出**：`inferlite/sampler/sampling.py`
**核心**：
- `SamplingParams` dataclass（temperature / top-k / top-p / penalty / seed）
- `SamplingProcessor.__call__(logits)` 实现 5 步采样流水线
- 接口兼容 `GreedySampler`（输入 `[B, V]`，输出 `[B, 1]`）

**验证**：
- `tests/unit/test_sampling.py`：15 tests 全绿
  - greedy 快速路径（temperature=0 → argmax）
  - temperature 影响分布（高温更随机，低温更集中）
  - top-k / top-p 过滤正确性
  - repetition penalty 降低重复 token 概率
  - seed 可复现测试
  - 接口兼容性（可替换 GreedySampler）

**DoD**：
- [x] SamplingParams + SamplingProcessor 实现
- [x] 15 tests 全绿
- [x] 与 GreedySampler 接口兼容
- [x] 注释覆盖采样流水线 5 步

---

### T2: OpenAI Schemas

**前置**：T1 完成
**产出**：`inferlite/server/schemas.py`
**核心**：
- `ChatCompletionRequest`（请求 schema）
- `ChatCompletionResponse`（非流式响应）
- `ChatCompletionStreamChunk`（流式 chunk）
- `UsageInfo` / `ResponseChoice` / `StreamChoice` 等子模型

**验证**：
- `tests/unit/test_openai_api.py::TestChatCompletionRequest`：4 tests
  - minimal request（只有 messages）
  - full request（所有字段）
  - temperature / top_p 边界校验
- `tests/unit/test_openai_api.py::TestChatCompletionResponse`：2 tests
  - 响应格式符合 OpenAI spec
  - reasoning_content 字段存在

**DoD**：
- [x] Pydantic models 对齐 OpenAI spec
- [x] 6 tests 全绿
- [x] 字段校验（temperature / top_p 边界）

---

### T3: AsyncEngine

**前置**：T1 完成
**产出**：`inferlite/engine/async_engine.py`
**核心**：
- `AsyncEngine.start()` 启动后台线程
- `AsyncEngine.submit(prompt_ids, sampling_params)` 提交请求，返回 `(request_id, output_queue)`
- 后台线程运行 `_background_loop()`：admit → prefill → decode → emit tokens
- `AsyncEngine.stop()` 停止后台线程
- 复用 M3/M4/M5 的 `FCFSScheduler` + `PagedCacheAdapter` + `batch_generate_loop` 逻辑

**验证**：
- 导入测试：`from inferlite.engine import AsyncEngine`
- 接口测试：`submit()` 返回 `(request_id, queue)`
- 后台线程启动/停止测试

**DoD**：
- [x] AsyncEngine 实现（~250 行）
- [x] 后台线程正确运行
- [x] submit() 接口线程安全
- [x] 复用 M3/M4/M5 的 cache + scheduler

---

### T4: FastAPI App + SSE

**前置**：T2 + T3 完成
**产出**：`inferlite/server/app.py`
**核心**：
- `create_app(engine)` 工厂函数，创建 FastAPI 实例
- `GET /health` 健康检查端点
- `POST /v1/chat/completions` 聊天补全端点
  - 非流式：`await _complete_response()`，等待全部 token，返回 `ChatCompletionResponse`
  - 流式：`StreamingResponse(_stream_response())`，SSE 逐 token 推送
- `_split_thinking()` 提取 Qwen3 thinking 内容
- `_stream_response()` 生成器：`yield f"data: {chunk_json}\n\n"` + `yield "data: [DONE]\n\n"`

**验证**：
- `tests/unit/test_openai_api.py::TestStreamChunk`：4 tests
  - chunk 格式符合 OpenAI spec
  - 首 chunk 包含 role=assistant
  - 结束 chunk 包含 finish_reason
  - SSE 格式 `data: {json}\n\n`
- `tests/unit/test_openai_api.py::TestSplitThinking`：3 tests
  - 无 thinking 标签
  - 有完整 `<think>...</think>`
  - 未闭合 `</think>`
- `tests/unit/test_openai_api.py::TestFastAPIApp`：2 tests
  - create_app 创建成功
  - health 端点返回 200

**DoD**：
- [x] FastAPI app 实现（~250 行）
- [x] /health + /v1/chat/completions 端点
- [x] SSE 流式输出格式正确
- [x] reasoning_content 映射正确
- [x] 9 tests 全绿

---

### T5: CLI (inferlite serve)

**前置**：T3 + T4 完成
**产出**：`inferlite/server/cli.py` + `pyproject.toml` 更新
**核心**：
- `inferlite serve --model-dir /path/to/model` 命令
- 参数：`--host` / `--port` / `--device` / `--dtype` / `--num-blocks` / `--block-size`
- `main()` 流程：
  1. 解析参数
  2. 加载 tokenizer + model
  3. 创建 AsyncEngine 并 start()
  4. 创建 FastAPI app
  5. uvicorn.run(app, host, port)
  6. finally: engine.stop()
- `pyproject.toml` 注册 `inferlite-serve = "inferlite.server.cli:main"`

**验证**：
- 导入测试：`from inferlite.server.cli import main`
- 参数解析测试：`parse_args(["--model-dir", "/path"])`
- 手动 smoke test：
  ```bash
  inferlite serve --model-dir ~/.cache/modelscope/hub/models/Qwen/Qwen3-0___6B
  curl http://localhost:8000/health
  curl http://localhost:8000/v1/chat/completions \
    -H "Content-Type: application/json" \
    -d '{"model":"qwen3","messages":[{"role":"user","content":"Hi"}]}'
  ```

**DoD**：
- [x] CLI 命令实现（~100 行）
- [x] pyproject.toml 注册 inferlite-serve
- [x] 参数解析正确
- [x] 服务启动成功

---

## 5. 与 vLLM V1 的简化对照

| vLLM V1 复杂度 | inferlite M6 简化 |
|----------------|-------------------|
| 多进程（API Server + EngineCore + GPU Workers） | 单进程（FastAPI + AsyncEngine 后台线程） |
| asyncio + multiprocessing.Queue | threading + queue.Queue |
| EngineCore 独立进程 | 后台线程 |
| Token-budget scheduling | 复用 M3 FCFS |
| Chunked prefill | 不做（M10） |
| CUDA Graph | 不做（M9） |
| FlashAttention | 不做（M9） |
| Multi-GPU TP/PP | 不做（M12+） |
| 量化 | 不做（M12+） |
| Web UI | 不做（可选 Gradio/Streamlit） |

---

## 6. 完成定义

- [x] SamplingParams + SamplingProcessor 实现（T1）
- [x] OpenAI schemas 实现（T2）
- [x] AsyncEngine 实现（T3）
- [x] FastAPI app + SSE 实现（T4）
- [x] CLI inferlite-serve 实现（T5）
- [x] 38 tests 全绿（15 + 6 + 9 + 7 流式思维链 + 1 streaming integration）
- [x] 351 tests 全量回归通过（313 已有 + 38 新增）
- [x] ruff lint 全部通过
- [x] 代码注释覆盖核心机制

---

## 7. 使用示例

### 启动服务

```bash
inferlite serve \
  --model-dir ~/.cache/modelscope/hub/models/Qwen/Qwen3-0___6B \
  --port 8000 \
  --device mps \
  --dtype bf16
```

### curl 非流式

```bash
curl http://localhost:8000/v1/chat/completions \
  -H "Content-Type: application/json" \
  -d '{
    "model": "qwen3",
    "messages": [{"role": "user", "content": "What is 1+1?"}],
    "temperature": 0.8,
    "max_tokens": 100
  }'
```

### curl 流式

```bash
curl http://localhost:8000/v1/chat/completions \
  -H "Content-Type: application/json" \
  -d '{
    "model": "qwen3",
    "messages": [{"role": "user", "content": "Explain quantum computing"}],
    "stream": true,
    "temperature": 0.7,
    "max_tokens": 500
  }'
```

### OpenAI Python SDK

```python
from openai import OpenAI

client = OpenAI(base_url="http://localhost:8000/v1", api_key="none")

# 非流式
response = client.chat.completions.create(
    model="qwen3",
    messages=[{"role": "user", "content": "What is 1+1?"}],
    temperature=0.8,
    max_tokens=100,
)
print(response.choices[0].message.content)

# 流式
response = client.chat.completions.create(
    model="qwen3",
    messages=[{"role": "user", "content": "Explain quantum computing"}],
    stream=True,
    temperature=0.7,
    max_tokens=500,
)
for chunk in response:
    if chunk.choices[0].delta.content:
        print(chunk.choices[0].delta.content, end="", flush=True)
```

---

## 8. ADR

### ADR-01：单进程 + 后台线程（对齐 vLLM V1 简化版）

**Context**：vLLM V1 使用多进程架构（API Server + EngineCore + GPU Workers），适合生产环境。inferlite 是教学项目，需要简化。

**Decision**：单进程 + 后台线程。Main Thread 运行 FastAPI，Background Thread 运行 engine loop，通过 `queue.Queue` 通信。

**Consequences**：
- 实现简单，无需进程间通信
- GIL 不是瓶颈（model forward 释放 GIL）
- 不适合多 GPU / 高并发生产场景
- 后续可扩展为多进程（M12+）

### ADR-02：复用 M3/M4/M5 的 cache + scheduler

**Context**：M3/M4/M5 已实现 FCFS scheduler + PagedCacheAdapter + prefix cache。M6 是否需要重新实现？

**Decision**：复用现有组件，AsyncEngine 只负责线程模型 + 动态请求接收。

**Consequences**：
- 代码复用，避免重复实现
- M3/M4/M5 的测试覆盖自动继承
- 不需要修改 cache / scheduler 核心逻辑

### ADR-03：SamplingProcessor 替代 GreedySampler

**Context**：M3 只有 GreedySampler（argmax）。M6 需要支持 temperature / top-k / top-p / repetition penalty。

**Decision**：新增 `SamplingProcessor`，接口兼容 `GreedySampler`（`__call__(logits) -> [B, 1]`），内部实现 5 步采样流水线。

**Consequences**：
- 接口兼容，可直接替换 GreedySampler
- 支持可配置采样策略
- GreedySampler 保留作为快速路径（temperature=0）

### ADR-04：SSE 流式输出（对齐 OpenAI）

**Context**：vLLM 使用 SSE（Server-Sent Events）流式推送 chunk。

**Decision**：FastAPI `StreamingResponse` + SSE 格式（`data: {json}\n\n` + `data: [DONE]\n\n`）。

**Consequences**：
- 对齐 OpenAI ChatCompletionStream 格式
- 客户端可直接使用 OpenAI SDK / EventSource
- 简单高效，无需 WebSocket

### ADR-05：reasoning_content 映射（Qwen3 特有）

**Context**：Qwen3 thinking 模式输出 `<think>...</think>` 标签。OpenAI spec 没有 `reasoning_content` 字段。

**Decision**：提取 `<think>...</think>` 内容，映射到自定义 `reasoning_content` 字段（非流式响应）。流式响应暂不推送 reasoning_content（简化实现）。

**Consequences**：
- 客户端可访问 reasoning_content（如果支持）
- 流式输出全部作为 content（简化）
- 后续可扩展流式 reasoning_content（可选）

---

## 9. 文件清单

| 文件 | 任务卡 | 类型 | 行数 |
|------|--------|------|------|
| `inferlite/sampler/sampling.py` | T1 | 新建 | ~150 |
| `inferlite/server/schemas.py` | T2 | 新建 | ~100 |
| `inferlite/engine/async_engine.py` | T3 | 新建 | ~250 |
| `inferlite/server/app.py` | T4 | 新建 | ~250 |
| `inferlite/server/cli.py` | T5 | 新建 | ~100 |
| `inferlite/server/__init__.py` | T4 | 修改 | ~10 |
| `inferlite/engine/__init__.py` | T3 | 修改 | ~5 |
| `inferlite/sampler/__init__.py` | T1 | 修改 | ~5 |
| `pyproject.toml` | T5 | 修改 | ~2 |
| `tests/unit/test_sampling.py` | T1 | 新建 | ~250 |
| `tests/unit/test_openai_api.py` | T2/T4 | 新建 | ~300 |
| **总计** | | | **~1422** |

---

## 10. 测试覆盖

| 测试文件 | 数量 | 覆盖范围 |
|----------|------|----------|
| `tests/unit/test_sampling.py` | 15 | SamplingParams + SamplingProcessor |
| `tests/unit/test_openai_api.py` | 23 | schemas + FastAPI + SSE + thinking + 流式思维链解析 |
| **M6 新增** | **38** | |
| **全量回归** | **351** | 313 已有 + 38 新增 |

---

## 11. 已知局限性及后续解决路径

| M6 当前短板 | 根因 | 解决里程碑 | 具体方案 |
|-------------|------|------------|----------|
| ~~流式输出不推送 reasoning_content~~ | ✅ 已支持（StreamingThinkParser 状态机） | — | — |
| 无 Web UI | CLI 足够 | 可选 T6 | Gradio / Streamlit |
| 单进程架构 | 教学简化 | M12+ | 多进程 + IPC |
| 无 token-budget scheduling | M3 FCFS 足够 | M10 | chunked prefill + token budget |
| 无量化 / 多 GPU | 不在 M6 范围 | M12+ | GPTQ / AWQ / TP / PP |

---

## 12. 后续 M7 入口

M6 完成后，inferlite 具备完整的推理服务能力：

- ✅ M1：Qwen3 模型实现 + 数值对齐
- ✅ M2：KV Cache（单请求加速）
- ✅ M3：Continuous Batching（多请求并发）
- ✅ M4：PagedAttention（显存分页管理）
- ✅ M5：Prefix Cache（前缀复用）
- ✅ M6：API + SSE（HTTP 服务化）

**M7 候选方向**：
- **MoE 模型支持**：Mixtral / DeepSeek-V2 等 MoE 模型
- **推测解码**：Speculative Decoding（n-gram / EAGLE）
- **长上下文**：Chunked Prefill + YaRN RoPE scaling
- **性能优化**：Triton kernel / FlashAttention / CUDA Graph

详见 `docs/plan/PLAN.md`。
